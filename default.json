{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Shared Renovate posture for folklorevault repos. Consume with: { \"extends\": [\"local>folklorevault/renovate-config\"] }",
  "extends": ["config:recommended", "group:allNonMajor", ":pinDevDependencies"],
  "timezone": "America/New_York",
  "schedule": ["before 5am on monday"],
  "constraints": { "pnpm": "<11" },
  "minimumReleaseAge": "3 days",
  "internalChecksFilter": "strict",
  "packageRules": [
    {
      "description": "Pin ESLint to the v9 maintenance line until eslint-config-next's bundled plugins (eslint-plugin-react, typescript-eslint) support ESLint 10's breaking context/scope-manager API changes. No-op in repos without ESLint.",
      "matchPackageNames": ["eslint"],
      "allowedVersions": "<10.0.0"
    },
    {
      "description": "Pin @babel/core to the v7 maintenance line. Transitive dep already patched at 7.29.6+ via a pnpm-workspace override; widening past v8 gives no security benefit and risks an unvalidated major. Lift only as a deliberate, tested babel 8 upgrade. No-op in repos without Babel.",
      "matchPackageNames": ["@babel/core"],
      "allowedVersions": "<8.0.0"
    },
    {
      "description": "Pin undici to the v7 maintenance line. Transitive Sanity-tooling dep already patched at 7.28.0+ via a pnpm-workspace override; the pin exists only to escape a vuln, not to track latest. Lift only as a deliberate, tested undici 8 upgrade. No-op in repos without undici.",
      "matchPackageNames": ["undici"],
      "allowedVersions": "<8.0.0"
    },
    {
      "description": "Auto-merge low-risk updates once CI passes. Majors are still opened as PRs for manual review. Combined with the weekly schedule, routine minor/patch bumps land themselves without ever needing a click.",
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "automerge": true
    },
    {
      "description": "Group all Sanity-ecosystem MAJOR updates into a single PR so the suite moves together instead of one PR per package. Non-major Sanity updates still flow through group:allNonMajor.",
      "matchUpdateTypes": ["major"],
      "matchPackageNames": [
        "sanity",
        "@sanity/**",
        "sanity-plugin-**",
        "next-sanity"
      ],
      "groupName": "sanity major updates"
    },
    {
      "description": "Group all ESLint-ecosystem MAJOR updates into a single PR. Non-major ESLint updates still flow through group:allNonMajor.",
      "matchUpdateTypes": ["major"],
      "matchPackageNames": [
        "eslint",
        "@eslint/**",
        "eslint-config-**",
        "eslint-plugin-**",
        "typescript-eslint",
        "@typescript-eslint/**"
      ],
      "groupName": "eslint major updates"
    }
  ],
  "osvVulnerabilityAlerts": true,
  "vulnerabilityAlerts": {
    "minimumReleaseAge": null,
    "schedule": [],
    "labels": ["security"],
    "vulnerabilityFixStrategy": "highest",
    "automerge": true
  },
  "prHourlyLimit": 2,
  "prConcurrentLimit": 10
}
